## Networking Fundamentals (TH ver.)

---

### **1. แบบจำลองเครือข่าย OSI (OSI Model)**

เป็นโมเดลมาตรฐาน 7 ชั้น ที่อธิบายว่า “ข้อมูลถูกส่งจากเครื่องหนึ่งไปยังอีกเครื่องหนึ่งได้อย่างไร”  
แต่ละชั้นมีหน้าที่เฉพาะของตัวเอง

|ลำดับ|ชื่อชั้น (Layer)|หน้าที่หลัก|ตัวอย่างโปรโตคอล|
|---|---|---|---|
|7|Application|โปรแกรมที่ผู้ใช้เรียกใช้|HTTP, DNS|
|6|Presentation|แปลงรูปแบบ/เข้ารหัสข้อมูล|SSL, JPEG|
|5|Session|จัดการ session การเชื่อมต่อ|NetBIOS|
|4|Transport|ควบคุมการรับส่งข้อมูล|TCP, UDP|
|3|Network|หาที่อยู่และเส้นทางส่ง|IP|
|2|Data Link|ตรวจจับข้อผิดพลาด, ใช้ MAC Address|Ethernet|
|1|Physical|ส่งข้อมูลทางสายหรือสัญญาณ|สาย LAN, Wi-Fi|

📘 **ตัวอย่างเปรียบเทียบง่าย ๆ:**  
เหมือน “การส่งจดหมาย” —  
เขียน (Application), แพ็ก (Presentation), จัดการส่ง (Session), ตรวจให้ถึงถูกคน (Transport), หาที่อยู่ (Network), ส่งผ่านไปรษณีย์ (Data Link), เคลื่อนย้ายจริง (Physical)

---

### **2. IP Address, Subnet, Routing**

**IP Address (ที่อยู่ไอพี):** หมายเลขเฉพาะของอุปกรณ์ในเครือข่าย เช่น `192.168.1.10`

**Subnet (ซับเน็ต):** การแบ่งเครือข่ายใหญ่ให้เป็นเครือข่ายย่อย เพื่อความปลอดภัยและจัดการง่าย เช่น  
`192.168.1.0/24` → มี 256 ที่อยู่ (0–255)

**Routing (การกำหนดเส้นทาง):**  
คือการหาทางที่ดีที่สุดให้ข้อมูลเดินทางไปถึงปลายทาง โดยมีอุปกรณ์ชื่อ **Router** เป็นตัวกำหนดเส้นทาง

---

### **3. TCP, UDP และหมายเลขพอร์ต (Port Numbers)**

|คุณสมบัติ|TCP|UDP|
|---|---|---|
|การเชื่อมต่อ|ต้องมีการเชื่อมต่อก่อน (3-way handshake)|ไม่ต้องเชื่อมต่อ|
|ความน่าเชื่อถือ|ส่งครบแน่นอน|ไม่รับประกัน|
|ความเร็ว|ช้ากว่า|เร็วกว่า|
|การใช้งาน|เว็บ, อีเมล, FTP|วิดีโอ, เกม, VoIP|
|ตัวอย่างพอร์ต|80 (HTTP), 443 (HTTPS)|53 (DNS), 69 (TFTP)|

🔢 **หมายเลขพอร์ต (Port Number):**  
คือ “ช่องทาง” ของแต่ละบริการ เช่น

- Port 80 → HTTP
    
- Port 443 → HTTPS
    
- Port 22 → SSH
    

---

### **4. การเชื่อมต่อกับพอร์ต TCP ผ่าน Command Line**

สามารถใช้คำสั่งทดสอบการเชื่อมต่อพอร์ต TCP ได้หลายวิธี เช่น

🧩 **1. Telnet**

`telnet <IP> <port> เช่น: telnet 8.8.8.8 53`

🧩 **2. Netcat (nc)**

`nc <IP> <port> เช่น: nc 192.168.1.1 80`

🧩 **3. PowerShell (Windows)**

`Test-NetConnection <IP> -Port <port> เช่น: Test-NetConnection google.com -Port 443`

✅ ถ้าเชื่อมต่อสำเร็จ = พอร์ตเปิด  
❌ ถ้าไม่สำเร็จ = พอร์ตถูกปิด หรือมี Firewall บล็อกอยู่
